"use client";
import React from 'react';
import { useCompleteCheckoutMutation } from '../../../../gql/graphql';
import {GetLoginUserDetailsDocument } from '../../../../gql/graphql-documents';

export const CheckoutComplete = () => {
  return (
    <div>CheckoutComplete</div>
  );
};