mutation AccountRegister(
  $input: AccountRegisterInput!
) {
  accountRegister(
    input: $input
  ) {
    errors {
      code
      field
      message
    }
    requiresConfirmation
    user {
      ...BasicUserDetails
    }
  }
}