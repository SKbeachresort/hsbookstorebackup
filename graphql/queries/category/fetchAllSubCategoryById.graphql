query fetchAllSubCategoryById($id: ID!) {
  category(id: $id) {
    ...CategoryDetails
    children(first: 20) {
      totalCount
      edges {
        cursor
        node {
         ...CategoryDetails 
        }
      }
    }
  }
}